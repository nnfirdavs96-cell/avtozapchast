Options -Indexes
DirectoryIndex index.php index.html

RewriteEngine On
RewriteBase /

# Block direct access to sensitive directories
RewriteRule ^config/ - [F,L]
RewriteRule ^sql/ - [F,L]
RewriteRule ^deploy/ - [F,L]
RewriteRule ^storage/backups/ - [F,L]
RewriteRule ^\.git/ - [F,L]

# SEO: serve the dynamic sitemap at the conventional /sitemap.xml
RewriteRule ^sitemap\.xml$ sitemap.php [L]

# SEO-friendly product URLs: /product/123-slug → catalog/part.php?id=123
# (slug is optional and cosmetic; routing matches on the numeric id)
RewriteRule ^product/([0-9]+)(?:-[^/]*)?/?$ catalog/part.php?id=$1 [L,QSA]

# Route all requests through index.php (same as nginx try_files)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

# Security headers
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>

# PHP settings for Timeweb
<IfModule mod_php.c>
    php_value upload_max_filesize 32M
    php_value post_max_size 32M
    php_value max_execution_time 60
    php_value memory_limit 256M
</IfModule>
